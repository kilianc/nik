## Missing final report

You stopped without sending a final `task_report`. Re-read the **Role** section and call `task_report` now with status `completed` or `failed` and a brief summary of the outcome.
